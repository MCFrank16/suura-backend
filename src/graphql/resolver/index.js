import { queryUser, mutationUser, users } from './user';
import { mutationSession, logout, logoutAll } from './session';

export default {
  mutationSession,
  logout,
  queryUser,
  mutationUser,
  logoutAll,
  users,
};
